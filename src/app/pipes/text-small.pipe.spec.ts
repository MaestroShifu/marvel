import { TextSmallPipe } from './text-small.pipe';

describe('TextSmallPipe', () => {
  it('create an instance', () => {
    const pipe = new TextSmallPipe();
    expect(pipe).toBeTruthy();
  });
});
